{
    "name": "zvvnmod/mongol-convert",
    "description": "Mongolian Encoding Converter — PHP FFI binding to the Rust mongol-convert core (verified byte-exact vs the original Java).",
    "type": "library",
    "license": "Apache-2.0",
    "keywords": ["mongolian", "encoding", "converter", "menk", "z52", "delehi", "ffi"],
    "require": {
        "php": ">=7.4",
        "ext-ffi": "*",
        "ext-mbstring": "*"
    },
    "autoload": {
        "psr-4": {
            "MongolConvert\\": "src/"
        }
    },
    "suggest": {
        "ext-ffi": "Enable ffi.enable=1 (or preload the lib) so the native converter can be called."
    }
}
